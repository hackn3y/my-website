{
  "name": "my-portfolio-shop",
  "version": "1.0.0",
  "description": "Portfolio and shop website",
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "sharp": "^0.34.4"
  },
  "scripts": {
    "test:smoke": "npx playwright test tests/smoke.spec.js --browser=chromium"
  }
}
